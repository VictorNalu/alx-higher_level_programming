#!/usr/bin/python3
def uppercase(str):
    for iterator in str:
        strin = iterator
        if ord(strin) >= 97 and ord(strin) <= 122:
            strin = chr (ord(iterator - 32)
                   print("{}".format(strin), )
     
